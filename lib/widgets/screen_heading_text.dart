import 'package:flutter/material.dart';

class ScreenHeadingText extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Text(
      'Explore',
      style: TextStyle(
        fontWeight: FontWeight.w900,
        fontSize: 35,
      ),
    );
  }
}
